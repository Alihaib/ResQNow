# ResQNow — local environment variables
# Copy this file to `.env` and fill in real values. Never commit `.env`.

# OpenAI API key used by the AI Emergency Companion feature.
# Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=REPLACE_ME_WITH_REAL_KEY
